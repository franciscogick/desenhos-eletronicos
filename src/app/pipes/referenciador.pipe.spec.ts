import { ReferenciadorPipe } from './referenciador.pipe';

describe('ReferenciadorPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenciadorPipe();
    expect(pipe).toBeTruthy();
  });
});
